global:
  scrape_interval: 15s

scrape_configs:
  - job_name: foundry-api
    metrics_path: /api/v1/metrics
    static_configs:
      - targets:
          # Update host/port for your deployment
          - localhost:9000
        labels:
          service: foundry-api
